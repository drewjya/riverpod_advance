name: riverpod_advance
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.16.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  hooks_riverpod: ^1.0.3
  flutter_hooks: ^0.18.3
  equatable: ^2.0.3
  sqflite: ^2.0.2
  hive: ^2.1.0
  hive_flutter: ^1.1.0
  intl: ^0.17.0
  file_picker: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/